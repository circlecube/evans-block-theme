// TODO: add logo image

// compare/contrast the approaches:

// Resume Experience
// resume-experience - resume block to list experience
// as a block variation
import './resume-experience';
// as a block pattern
// as a block template
// as a custom block

// Resume Skills
// resume-skills - resume block to list skills and stars
// as a block style on core list block - need to add stars manually
// as a block with skill nested in skills parent block - stars can be built into block
// - in `./blocks/skill/` and `./block/skills/`

// Talk
// talk - block for speaking experience
// as a block pattern
// as a block variation
// as a block template
// as a custom block - in `./blocks/talk/`